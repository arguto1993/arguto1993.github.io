# Production env — used by `npm run build` (locally and in CI).
# These are NOT secrets: VITE_* values get baked into the public JS bundle.
# The actual OAuth client_secret lives only on the Cloudflare Worker.

VITE_GH_CLIENT_ID=Ov23liyDcji00IGCxbiE
VITE_OAUTH_WORKER_URL=https://arguto-portfolio-oauth.argo-wahyu-utomo.workers.dev
